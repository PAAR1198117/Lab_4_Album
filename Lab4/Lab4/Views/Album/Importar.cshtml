@model IEnumerable<Lab4.Models.Estampitas1>

@{
    ViewBag.Title = "Importar";
}

<h2>Importar</h2>

<p>
    @using (Html.BeginForm("Importar", "Album", FormMethod.Post, new { enctype = "multipart/form-data" }))
    {
        <input type="file" name="postedFile" />
        <input type="submit" value="Importar" />
    }
</p>
<table class="table">
    <tr>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>

        </td>
    </tr>
}

</table>
